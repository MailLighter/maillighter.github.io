---
import MarketingLayout from '@/layouts/MarketingLayout.astro';
import { siteConfig } from '@/config';
---

<MarketingLayout
  title={`${siteConfig.name} — Lighten your emails, lighten your impact`}
  description="The Outlook plugin that cleans your emails in one click. For clearer exchanges, lighter servers, and concrete digital sobriety."
>
  <!-- ════ HERO ════ -->
  <section class="landing-hero">
    <div class="hero-grid">
      <div class="hero-left">
        <span class="hero-badge">
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none" stroke="var(--ml-blue)" stroke-width="1.8"><path d="M6 1v10M3 8l3 3 3-3" stroke-linecap="round"/></svg>
          Free Outlook Add-in&nbsp;<span class="badge-accent">No account</span>
        </span>
        <h1 class="hero-title">
          <span class="accent-blue">Lighten</span> your emails.<br>
          <span class="accent-green">Lighten your impact.</span>
        </h1>
        <p class="hero-sub">MailLighter removes useless images, forgotten attachments and endless reply chains from your emails — in one click, directly from the Outlook ribbon.</p>
        <div class="hero-ctas">
          <a href="https://appsource.microsoft.com/" class="btn-primary">
            <svg width="15" height="15" viewBox="0 0 15 15" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M7.5 2v8M5 7l2.5 3 2.5-3M2 12h11"/></svg>
            Install for free
          </a>
          <a href="#features" class="btn-ghost">See how it works →</a>
        </div>
        <div class="hero-trust">
          <a href="https://appsource.microsoft.com/" class="appsource-badge">
            <svg width="16" height="16" viewBox="0 0 16 16"><rect width="7" height="7" rx="1" fill="#f25022"/><rect x="9" width="7" height="7" rx="1" fill="#7fba00"/><rect y="9" width="7" height="7" rx="1" fill="#00a4ef"/><rect x="9" y="9" width="7" height="7" rx="1" fill="#ffb900"/></svg>
            Microsoft AppSource
          </a>
          <span class="trust-sep">·</span>
          <span class="trust-text">Outlook Desktop · Windows & macOS · Microsoft 365</span>
        </div>
      </div>
      <div class="hero-right">
        <div class="outlook-mockup">
          <div class="mock-titlebar">
            <div class="tbar-dot"></div><div class="tbar-dot"></div><div class="tbar-dot"></div>
            <span class="tbar-title">New Email — Microsoft Outlook</span>
          </div>
          <div class="mock-ribbon">
            <div class="rbtn">Send</div>
            <div class="rbtn">Attach</div>
            <div class="rsep"></div>
            <div class="rbtn blue">⚡ MailLighter</div>
            <div class="rbtn green-outline">Full cleanup</div>
            <div class="rbtn">Images</div>
            <div class="rbtn">2 replies</div>
          </div>
          <div class="mock-body">
            <div class="mock-header">
              <div class="mock-row"><span class="mock-lbl">To:</span><span class="mock-val">team@company.com (12 recipients)</span></div>
              <div class="mock-row"><span class="mock-lbl">Sub:</span><span class="mock-val">Re: Q4 Report — Final review</span></div>
            </div>
            <div class="mock-text">
              <p style="margin-bottom:8px">Hi all, please find below the updated version for review.</p>
              <div class="mock-asset"><span class="mock-asset-icon">🖼️</span> company_banner.png — <strong>820 KB</strong></div>
              <div class="mock-asset"><span class="mock-asset-icon">📎</span> report_Q4_final_v3.xlsx — <strong>3.4 MB</strong></div>
              <div class="mock-quoted">— Original message (8 replies, 3 weeks of history) —<br>From: Marc · 14 Nov · Sent: Re: Re: Re: Re: Q4 report…</div>
            </div>
            <div class="mock-result">
              <span class="mock-result-icon">✅</span>
              <div><strong>Full cleanup complete</strong><br>Images: 1 removed (820 KB) · Attachments: 1 removed (3.4 MB)<br>Replies: 8 → 2 kept · <strong>Total saved: 4.2 MB</strong></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════ STATS ════ -->
  <section class="stats-band">
    <div class="stats-inner">
      <div class="stat-item">
        <div class="stat-num">300<span class="stat-unit">Bn</span></div>
        <div class="stat-label">emails sent every day worldwide</div>
        <span class="stat-badge">daily volume</span>
      </div>
      <div class="stat-item">
        <div class="stat-num">3–<span class="stat-unit">4%</span></div>
        <div class="stat-label">of global greenhouse gas emissions from digital</div>
        <span class="stat-badge">GHG impact</span>
      </div>
      <div class="stat-item">
        <div class="stat-num">~<span class="stat-unit">200×</span></div>
        <div class="stat-label">less data in transit after a MailLighter cleanup</div>
        <span class="stat-badge">savings</span>
      </div>
    </div>
  </section>

  <!-- ════ BEFORE / AFTER ════ -->
  <section class="ba-section">
    <div class="ba-header">
      <span class="section-eyebrow green">Real impact</span>
      <h2 class="section-title">One click. A measurable difference.</h2>
      <p class="section-sub">The concrete transformation on a real email.</p>
    </div>
    <div class="ba-cards">
      <div class="ba-card">
        <div class="ba-label">📧 Before</div>
        <div class="ba-size red">4.2 MB</div>
        <div class="ba-details">12 embedded images<br>3 forgotten attachments<br>8 weeks of reply history</div>
        <span class="ba-chip bad">🌡️ ≈ 12 g CO₂</span>
      </div>
      <div class="ba-divider">
        <div class="ba-arrow-circle">→</div>
        <div class="ba-ratio">200×<br>lighter</div>
      </div>
      <div class="ba-card after">
        <div class="ba-label green">✉️ After MailLighter</div>
        <div class="ba-size green">18 KB</div>
        <div class="ba-details">Text only<br>2 most recent replies kept<br>Nothing unnecessary</div>
        <span class="ba-chip good">🌿 ≈ 0.05 g CO₂</span>
      </div>
    </div>
  </section>

  <!-- ════ FEATURES ════ -->
  <section class="features-section" id="features">
    <div class="features-inner">
      <span class="section-eyebrow">Features</span>
      <h2 class="section-title">Five actions. One click.</h2>
      <p class="section-sub">Everything accessible directly from the Outlook ribbon. No configuration needed.</p>
      <div class="features-grid">
        <div class="fcard featured">
          <div class="fcard-icon green"><svg viewBox="0 0 20 20"><circle cx="10" cy="10" r="8"/><polyline points="7,10 9,12 13,8"/></svg></div>
          <span class="fcard-tag green">⭐ Most used</span>
          <div class="fcard-name">Full Cleanup</div>
          <div class="fcard-desc">Runs all actions at once. Images, attachments, reply threads — cleaned in one go. A detailed summary shows exactly what was removed and how much space was saved.</div>
        </div>
        <div class="fcard">
          <div class="fcard-icon"><svg viewBox="0 0 20 20"><rect x="2" y="2" width="16" height="12" rx="2"/><line x1="7" y1="18" x2="13" y2="18"/><line x1="10" y1="14" x2="10" y2="18"/></svg></div>
          <span class="fcard-tag">Smart</span>
          <div class="fcard-name">Remove Images</div>
          <div class="fcard-desc">Strips all embedded images from the body. Signature images are preserved.</div>
        </div>
        <div class="fcard">
          <div class="fcard-icon"><svg viewBox="0 0 20 20"><path d="M13 2H6a2 2 0 00-2 2v14l6-3 6 3V4a2 2 0 00-2-2z"/></svg></div>
          <span class="fcard-tag">Safe</span>
          <div class="fcard-name">Remove Attachments</div>
          <div class="fcard-desc">Removes attached files from the thread. Inline signature images are preserved.</div>
        </div>
        <div class="fcard">
          <div class="fcard-icon"><svg viewBox="0 0 20 20"><polyline points="15,10 10,15 5,10"/><line x1="10" y1="4" x2="10" y2="15"/></svg></div>
          <span class="fcard-tag">Multi-language</span>
          <div class="fcard-name">Keep 2 Replies</div>
          <div class="fcard-desc">Trims long threads to the 2 most recent messages. Detects quoted blocks in all languages.</div>
        </div>
        <div class="fcard">
          <div class="fcard-icon"><svg viewBox="0 0 20 20"><path d="M17 3a2.828 2.828 0 11-4 4L6 14H3v-3L10 4a2.828 2.828 0 014-1z"/></svg></div>
          <span class="fcard-tag">Surgical</span>
          <div class="fcard-name">Keep Selection</div>
          <div class="fcard-desc">Replace the entire thread with only the text you selected. Total precision.</div>
        </div>
        <div class="fcard">
          <div class="fcard-icon"><svg viewBox="0 0 20 20"><rect x="3" y="11" width="14" height="8" rx="2"/><path d="M7 11V7a5 5 0 0110 0v4"/><circle cx="10" cy="15" r="1" fill="#93c5fd"/></svg></div>
          <span class="fcard-tag">Privacy first</span>
          <div class="fcard-name">100% Local</div>
          <div class="fcard-desc">Runs entirely inside Outlook via Office.js. No data ever leaves your device.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════ SOCIAL PROOF ════ -->
  <section class="social-section">
    <span class="section-eyebrow">Reviews</span>
    <h2 class="section-title">Trusted by teams who care.</h2>
    <p class="section-sub">Used by thousands of professionals who have adopted the responsible email habit.</p>
    <div class="testimonials">
      <div class="tcard">
        <div class="stars">★★★★★</div>
        <p class="tcard-text">"Finally a tool that does something <strong>concrete and immediate</strong> for the digital carbon footprint. It takes 2 seconds, and the result is visible."</p>
        <div class="tcard-author"><div class="avatar">SL</div><div><div class="author-name">Sophie L.</div><div class="author-role">Sustainability Manager</div></div></div>
      </div>
      <div class="tcard">
        <div class="stars">★★★★★</div>
        <p class="tcard-text">"I use Full Cleanup on every email before sending. <strong>I save several MBs a day</strong> without even thinking about it."</p>
        <div class="tcard-author"><div class="avatar">TM</div><div><div class="author-name">Thomas M.</div><div class="author-role">Project Manager</div></div></div>
      </div>
      <div class="tcard">
        <div class="stars">★★★★☆</div>
        <p class="tcard-text">"Simple, effective, zero learning curve. I wish all Outlook plugins were this <strong>well thought-out</strong>."</p>
        <div class="tcard-author"><div class="avatar">AM</div><div><div class="author-name">Alice M.</div><div class="author-role">IT Director</div></div></div>
      </div>
    </div>
  </section>

  <!-- ════ HOW IT WORKS ════ -->
  <section class="hiw-section">
    <div class="hiw-inner">
      <span class="section-eyebrow">Getting started</span>
      <h2 class="section-title">Up and running in 60 seconds.</h2>
      <p class="section-sub">No account to create, no complex setup.</p>
      <div class="hiw-steps">
        <div class="hiw-step">
          <div class="step-circle">1</div>
          <div class="step-title">Install the add-in</div>
          <div class="step-desc">Download MailLighter from Microsoft AppSource and add it to Outlook in seconds.</div>
        </div>
        <div class="hiw-connector">→</div>
        <div class="hiw-step">
          <div class="step-circle">2</div>
          <div class="step-title">Compose your email</div>
          <div class="step-desc">Write or reply as usual. The MailLighter menu appears in your ribbon.</div>
        </div>
        <div class="hiw-connector">→</div>
        <div class="hiw-step">
          <div class="step-circle">3</div>
          <div class="step-title">Lighten before sending</div>
          <div class="step-desc">Choose your action. MailLighter runs instantly and shows you what was cleaned.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════ FINAL CTA ════ -->
  <section class="final-cta-wrap">
    <div class="final-cta-box">
      <span class="section-eyebrow">Ready?</span>
      <h2 class="section-title">Start lightening your emails today.</h2>
      <p class="section-sub">Free, no account, no data collection. Install MailLighter in seconds.</p>
      <a href="https://appsource.microsoft.com/" class="btn-cta">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M8 2v9M5 8l3 3 3-3M2 13h12"/></svg>
        Install for free on AppSource
      </a>
      <div class="cta-pills">
        <span class="cpill"><span class="cpill-dot"></span> Outlook Desktop</span>
        <span class="cpill"><span class="cpill-dot"></span> Windows & macOS</span>
        <span class="cpill"><span class="cpill-dot"></span> Microsoft 365 & Exchange</span>
        <span class="cpill"><span class="cpill-dot"></span> 100% Free</span>
      </div>
    </div>
  </section>
</MarketingLayout>

<style>
  /* ══════════════════════════════
     LANDING PAGE — scoped styles
     ══════════════════════════════ */

  /* Animations */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ── HERO ── */
  .landing-hero {
    max-width: 1200px;
    margin: 0 auto;
    padding: 120px 52px 80px;
  }
  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 56px;
  }
  .hero-left { display: flex; flex-direction: column; gap: 24px; }
  .hero-badge {
    display: inline-flex; align-items: center; gap: 7px;
    background: var(--ml-blue-pale); border: 1px solid var(--ml-blue-light); color: var(--ml-blue);
    font-size: 0.75rem; font-weight: 700; padding: 5px 12px; border-radius: 6px;
    width: fit-content; letter-spacing: 0.05em; text-transform: uppercase;
    animation: fadeUp 0.5s ease both;
  }
  .badge-accent { background: var(--ml-green); color: white; padding: 1px 7px; border-radius: 4px; font-size: 0.7rem; }
  .hero-title {
    font-size: clamp(2rem, 3.5vw, 3rem); font-weight: 800;
    line-height: 1.1; color: var(--ml-ink); animation: fadeUp 0.5s 0.1s ease both;
  }
  .accent-blue { color: var(--ml-blue); }
  .accent-green { color: var(--ml-green); font-style: italic; }
  .hero-sub {
    font-size: 1.05rem; line-height: 1.75; color: var(--ml-ink-soft);
    max-width: 460px; animation: fadeUp 0.5s 0.2s ease both;
  }
  .hero-ctas { display: flex; align-items: center; gap: 14px; flex-wrap: wrap; animation: fadeUp 0.5s 0.3s ease both; }

  .btn-primary {
    display: inline-flex; align-items: center; gap: 8px;
    background: var(--ml-blue); color: white;
    padding: 13px 26px; border-radius: 8px; font-weight: 600; font-size: 0.95rem;
    text-decoration: none; box-shadow: 0 4px 16px rgba(21,100,192,0.3);
    transition: background 0.22s, transform 0.22s, box-shadow 0.22s;
  }
  .btn-primary:hover { background: var(--ml-blue-dark); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(21,100,192,0.35); }

  .btn-ghost {
    display: inline-flex; align-items: center; gap: 6px;
    color: var(--ml-ink-soft); font-weight: 500; font-size: 0.9rem;
    text-decoration: none; border: 1px solid var(--color-border);
    padding: 12px 22px; border-radius: 8px;
    background: var(--color-surface); transition: border-color 0.2s, color 0.2s;
  }
  .btn-ghost:hover { border-color: var(--ml-blue); color: var(--ml-blue); }

  .hero-trust { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; animation: fadeUp 0.5s 0.4s ease both; }
  .appsource-badge {
    display: inline-flex; align-items: center; gap: 7px;
    background: var(--color-surface); border: 1px solid var(--color-border);
    border-radius: 7px; padding: 7px 14px;
    font-size: 0.78rem; font-weight: 600; color: var(--ml-ink); text-decoration: none;
    transition: border-color 0.2s;
  }
  .appsource-badge:hover { border-color: var(--ml-blue); }
  .trust-sep { color: var(--color-border); }
  .trust-text { font-size: 0.78rem; color: var(--ml-ink-mute); }

  /* ── OUTLOOK MOCKUP ── */
  .hero-right { animation: fadeUp 0.5s 0.15s ease both; }
  .outlook-mockup {
    background: var(--ml-mock-bg); border-radius: 14px; overflow: hidden;
    box-shadow: 0 20px 70px rgba(0,0,0,0.18), 0 4px 16px rgba(0,0,0,0.08);
    border: 1px solid var(--ml-mock-border);
  }
  .mock-titlebar { background: #1e3a5f; padding: 9px 14px; display: flex; align-items: center; gap: 6px; }
  .tbar-dot { width: 9px; height: 9px; border-radius: 50%; background: rgba(255,255,255,0.25); }
  .tbar-title { color: rgba(255,255,255,0.85); font-size: 0.75rem; font-weight: 500; margin-left: 8px; }
  .mock-ribbon {
    background: var(--ml-mock-ribbon); padding: 6px 10px;
    display: flex; align-items: center; gap: 5px;
    border-bottom: 1px solid var(--ml-mock-ribbon-border); flex-wrap: wrap;
  }
  .rbtn {
    background: var(--ml-mock-rbtn); border: 1px solid var(--ml-mock-border);
    border-radius: 4px; padding: 4px 9px; font-size: 0.7rem;
    color: var(--ml-mock-rbtn-c); white-space: nowrap;
  }
  .rbtn.blue { background: var(--ml-blue); border-color: var(--ml-blue); color: white; font-weight: 700; }
  .rbtn.green-outline { border-color: var(--ml-green); color: var(--ml-green); font-weight: 600; }
  .rsep { width: 1px; height: 18px; background: var(--ml-mock-border); margin: 0 3px; }
  .mock-body { background: var(--ml-mock-body); padding: 18px 20px; }
  .mock-header { padding-bottom: 12px; margin-bottom: 12px; border-bottom: 1px solid var(--color-border); display: flex; flex-direction: column; gap: 5px; }
  .mock-row { display: flex; gap: 8px; font-size: 0.76rem; }
  .mock-lbl { color: var(--ml-ink-mute); min-width: 36px; }
  .mock-val { color: var(--ml-ink); font-weight: 500; }
  .mock-text { font-size: 0.8rem; color: var(--ml-ink-soft); line-height: 1.6; }
  .mock-asset {
    background: var(--ml-mock-asset-bg); border: 1px dashed var(--ml-mock-asset-border);
    border-radius: 6px; padding: 8px 12px;
    display: flex; align-items: center; gap: 8px;
    font-size: 0.72rem; color: var(--ml-ink-mute); margin: 8px 0;
  }
  .mock-asset-icon { font-size: 1rem; }
  .mock-quoted { font-size: 0.72rem; color: var(--ml-ink-mute); border-left: 2px solid var(--color-border); padding-left: 8px; margin-top: 8px; line-height: 1.6; }
  .mock-result {
    margin-top: 12px; background: var(--ml-green-pale); border: 1px solid var(--ml-green-light);
    border-radius: 8px; padding: 10px 14px;
    display: flex; align-items: flex-start; gap: 8px;
    font-size: 0.73rem; line-height: 1.6; color: var(--ml-ink-soft);
  }
  .mock-result strong { color: var(--ml-green-dark); }
  :global(.dark) .mock-result strong { color: var(--ml-green); }
  .mock-result-icon { font-size: 1rem; flex-shrink: 0; }

  /* ── STATS BAND ── */
  .stats-band {
    background: linear-gradient(135deg, var(--ml-blue-deeper) 0%, var(--ml-blue-dark) 50%, #1a5c8a 100%);
    padding: 60px 52px;
  }
  .stats-inner { display: flex; max-width: 960px; margin: 0 auto; }
  .stat-item {
    flex: 1; display: flex; flex-direction: column; align-items: center; gap: 6px;
    padding: 0 36px; text-align: center; position: relative;
  }
  .stat-item:not(:last-child)::after {
    content: ''; position: absolute; right: 0; top: 10%; height: 80%;
    width: 1px; background: rgba(255,255,255,0.15);
  }
  .stat-num { font-size: clamp(2.2rem, 3vw, 3rem); color: white; line-height: 1; font-weight: 800; }
  .stat-unit { color: var(--ml-green); }
  .stat-label { font-size: 0.83rem; color: rgba(255,255,255,0.65); line-height: 1.5; max-width: 150px; }
  .stat-badge {
    display: inline-block; background: rgba(74,162,81,0.25); color: #86efac;
    font-size: 0.68rem; font-weight: 700; padding: 2px 9px; border-radius: 4px;
    letter-spacing: 0.06em; text-transform: uppercase;
  }

  /* ── SECTION COMMONS ── */
  .section-eyebrow {
    display: inline-flex; align-items: center; gap: 6px;
    background: var(--ml-blue-pale); color: var(--ml-blue);
    font-size: 0.72rem; font-weight: 700; padding: 4px 12px; border-radius: 6px;
    letter-spacing: 0.07em; text-transform: uppercase; margin-bottom: 14px;
  }
  .section-eyebrow.green { background: var(--ml-green-pale); color: var(--ml-green-dark); }
  :global(.dark) .section-eyebrow.green { color: var(--ml-green); }
  .section-title {
    font-size: clamp(1.9rem, 3vw, 2.8rem); font-weight: 800;
    color: var(--ml-ink); line-height: 1.15; margin-bottom: 14px;
  }
  .section-sub { font-size: 1rem; color: var(--ml-ink-soft); line-height: 1.7; max-width: 520px; margin-bottom: 48px; }

  /* ── BEFORE / AFTER ── */
  .ba-section { padding: 100px 52px; max-width: 1100px; margin: 0 auto; }
  .ba-header { text-align: center; }
  .ba-header .section-sub { margin: 0 auto 48px; }
  .ba-cards { display: grid; grid-template-columns: 1fr 56px 1fr; align-items: center; }
  .ba-card {
    background: var(--color-surface); border-radius: 16px; padding: 36px 32px;
    border: 1px solid var(--color-border); box-shadow: 0 4px 24px var(--ml-shadow);
    transition: transform 0.3s;
  }
  .ba-card:hover { transform: translateY(-4px); }
  .ba-card.after { border: 2px solid var(--ml-green); box-shadow: 0 8px 32px rgba(74,162,81,0.15); }
  .ba-label {
    font-size: 0.7rem; font-weight: 700; letter-spacing: 0.1em;
    text-transform: uppercase; margin-bottom: 14px;
    display: flex; align-items: center; gap: 6px; color: var(--ml-ink-mute);
  }
  .ba-label.green { color: var(--ml-green-dark); }
  :global(.dark) .ba-label.green { color: var(--ml-green); }
  .ba-size { font-size: 3.2rem; line-height: 1; font-weight: 800; }
  .ba-size.red { color: #dc2626; }
  .ba-size.green { color: var(--ml-green); }
  .ba-details { font-size: 0.82rem; color: var(--ml-ink-soft); line-height: 2; margin: 14px 0; }
  .ba-chip { display: inline-flex; align-items: center; gap: 5px; font-size: 0.78rem; font-weight: 600; padding: 5px 12px; border-radius: 6px; }
  .ba-chip.bad { background: #fef2f2; color: #dc2626; }
  :global(.dark) .ba-chip.bad { background: rgba(220,38,38,0.15); }
  .ba-chip.good { background: var(--ml-green-pale); color: var(--ml-green-dark); }
  :global(.dark) .ba-chip.good { color: var(--ml-green); }
  .ba-divider { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 6px; }
  .ba-arrow-circle {
    width: 44px; height: 44px; background: var(--ml-blue); border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: white; font-size: 1.1rem; box-shadow: 0 4px 16px rgba(21,100,192,0.3);
  }
  .ba-ratio { font-size: 0.68rem; font-weight: 700; color: var(--ml-blue); text-align: center; line-height: 1.3; }

  /* ── FEATURES ── */
  .features-section { background: var(--ml-blue-deeper); padding: 100px 52px; position: relative; overflow: hidden; }
  .features-section::after {
    content: ''; position: absolute; bottom: -100px; right: -100px;
    width: 400px; height: 400px; border-radius: 50%;
    background: radial-gradient(circle, rgba(74,162,81,0.12), transparent 70%); pointer-events: none;
  }
  .features-inner { max-width: 1100px; margin: 0 auto; }
  .features-section .section-eyebrow { background: rgba(255,255,255,0.1); color: rgba(255,255,255,0.8); }
  .features-section .section-title { color: white; }
  .features-section .section-sub { color: rgba(255,255,255,0.6); }
  .features-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
  .fcard {
    background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08);
    border-radius: 14px; padding: 26px 22px; transition: all 0.3s;
  }
  .fcard:hover { background: rgba(255,255,255,0.09); border-color: rgba(74,162,81,0.4); transform: translateY(-3px); }
  .fcard.featured { border-color: rgba(74,162,81,0.5); background: rgba(74,162,81,0.08); grid-column: span 2; }
  .fcard-icon { width: 42px; height: 42px; background: rgba(21,100,192,0.25); border-radius: 10px; display: flex; align-items: center; justify-content: center; margin-bottom: 14px; }
  .fcard-icon svg { width: 20px; height: 20px; stroke: #93c5fd; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
  .fcard-icon.green { background: rgba(74,162,81,0.25); }
  .fcard-icon.green svg { stroke: #86efac; }
  .fcard-tag { display: inline-block; font-size: 0.67rem; font-weight: 700; letter-spacing: 0.06em; text-transform: uppercase; padding: 2px 8px; border-radius: 4px; margin-bottom: 8px; background: rgba(21,100,192,0.2); color: #93c5fd; }
  .fcard-tag.green { background: rgba(74,162,81,0.2); color: #86efac; }
  .fcard-name { font-size: 1.15rem; font-weight: 700; color: white; margin-bottom: 6px; }
  .fcard-desc { font-size: 0.83rem; color: rgba(255,255,255,0.55); line-height: 1.65; }

  /* ── SOCIAL PROOF ── */
  .social-section { padding: 100px 52px; max-width: 1100px; margin: 0 auto; text-align: center; }
  .social-section .section-sub { margin: 0 auto 48px; }
  .testimonials { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; text-align: left; }
  .tcard {
    background: var(--color-surface); border-radius: 14px; padding: 26px 22px;
    box-shadow: 0 4px 20px var(--ml-shadow); border: 1px solid var(--color-border);
    transition: transform 0.3s, box-shadow 0.3s;
  }
  .tcard:hover { transform: translateY(-3px); box-shadow: 0 10px 32px var(--ml-shadow); }
  .stars { color: #f59e0b; font-size: 0.85rem; letter-spacing: 2px; margin-bottom: 10px; }
  .tcard-text { font-size: 0.86rem; color: var(--ml-ink-soft); line-height: 1.7; margin-bottom: 16px; font-style: italic; }
  .tcard-text strong { color: var(--ml-blue); font-style: normal; }
  .tcard-author { display: flex; align-items: center; gap: 10px; }
  .avatar { width: 36px; height: 36px; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-weight: 700; font-size: 0.82rem; background: linear-gradient(135deg, var(--ml-blue), var(--ml-blue-dark)); }
  .author-name { font-size: 0.82rem; font-weight: 600; color: var(--ml-ink); }
  .author-role { font-size: 0.74rem; color: var(--ml-ink-mute); }

  /* ── HOW IT WORKS ── */
  .hiw-section {
    background: var(--ml-blue-pale);
    border-top: 1px solid var(--ml-blue-light);
    border-bottom: 1px solid var(--ml-blue-light);
    padding: 100px 52px; text-align: center;
  }
  :global(.dark) .hiw-section { background: #0f1520; border-color: #1e2a40; }
  .hiw-inner { max-width: 900px; margin: 0 auto; }
  .hiw-section .section-sub { margin: 0 auto 48px; }
  .hiw-steps { display: grid; grid-template-columns: 1fr auto 1fr auto 1fr; gap: 12px; align-items: start; margin-top: 56px; }
  .step-circle {
    width: 54px; height: 54px; background: var(--ml-blue); color: white; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.5rem; font-weight: 800;
    margin: 0 auto 16px; box-shadow: 0 6px 20px rgba(21,100,192,0.3);
  }
  .step-title { font-weight: 700; color: var(--ml-ink); margin-bottom: 8px; font-size: 0.95rem; }
  .step-desc { font-size: 0.81rem; color: var(--ml-ink-soft); line-height: 1.6; }
  .hiw-connector { color: var(--ml-blue); font-size: 1.5rem; margin-top: 18px; opacity: 0.4; }

  /* ── FINAL CTA ── */
  .final-cta-wrap { padding: 80px 52px; }
  .final-cta-box {
    max-width: 820px; margin: 0 auto;
    background: linear-gradient(135deg, var(--ml-blue-deeper) 0%, var(--ml-blue-dark) 60%, #1a6b3c 100%);
    border-radius: 24px; padding: 64px 56px; text-align: center;
    position: relative; overflow: hidden; box-shadow: 0 24px 80px rgba(21,100,192,0.25);
  }
  .final-cta-box::before { content: ''; position: absolute; top: -80px; right: -80px; width: 280px; height: 280px; border-radius: 50%; background: radial-gradient(circle, rgba(74,162,81,0.18), transparent 70%); }
  .final-cta-box::after { content: ''; position: absolute; bottom: -60px; left: -60px; width: 220px; height: 220px; border-radius: 50%; background: radial-gradient(circle, rgba(21,100,192,0.3), transparent 70%); }
  .final-cta-box .section-eyebrow { background: rgba(255,255,255,0.12); color: rgba(255,255,255,0.9); position: relative; z-index: 1; }
  .final-cta-box .section-title { color: white; font-size: 2.4rem; position: relative; z-index: 1; }
  .final-cta-box .section-sub { color: rgba(255,255,255,0.68); margin: 0 auto 36px; position: relative; z-index: 1; }
  .btn-cta {
    display: inline-flex; align-items: center; gap: 8px;
    background: var(--ml-green); color: white;
    padding: 15px 36px; border-radius: 8px; font-weight: 700; font-size: 1rem;
    text-decoration: none; transition: all 0.25s;
    box-shadow: 0 4px 20px rgba(74,162,81,0.4); position: relative; z-index: 1;
  }
  .btn-cta:hover { background: var(--ml-green-dark); transform: translateY(-2px); box-shadow: 0 8px 28px rgba(74,162,81,0.5); }
  .cta-pills { display: flex; align-items: center; justify-content: center; gap: 8px; flex-wrap: wrap; margin-top: 22px; position: relative; z-index: 1; }
  .cpill { display: flex; align-items: center; gap: 5px; font-size: 0.76rem; color: rgba(255,255,255,0.65); background: rgba(255,255,255,0.07); border: 1px solid rgba(255,255,255,0.12); padding: 4px 12px; border-radius: 5px; }
  .cpill-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--ml-green); }

  /* ── RESPONSIVE ── */
  @media (max-width: 768px) {
    .landing-hero { padding: 100px 20px 60px; }
    .hero-grid { grid-template-columns: 1fr; gap: 40px; }
    .stats-band { padding: 40px 20px; }
    .stats-inner { flex-direction: column; gap: 32px; }
    .stat-item:not(:last-child)::after { display: none; }
    .ba-section { padding: 60px 20px; }
    .ba-cards { grid-template-columns: 1fr; gap: 20px; }
    .ba-divider { flex-direction: row; }
    .features-section { padding: 60px 20px; }
    .features-grid { grid-template-columns: 1fr; }
    .fcard.featured { grid-column: span 1; }
    .social-section { padding: 60px 20px; }
    .testimonials { grid-template-columns: 1fr; }
    .hiw-section { padding: 60px 20px; }
    .hiw-steps { grid-template-columns: 1fr; gap: 24px; }
    .hiw-connector { display: none; }
    .final-cta-wrap { padding: 40px 20px; }
    .final-cta-box { padding: 40px 24px; }
  }
</style>
