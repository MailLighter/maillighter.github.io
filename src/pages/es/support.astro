---
import MarketingLayout from '@/layouts/MarketingLayout.astro';
import { siteConfig } from '@/config';

const accessKey = import.meta.env.PUBLIC_WEB3FORMS_KEY || '';
---

<MarketingLayout
  title={`Soporte — ${siteConfig.name}`}
  description="¿Necesita ayuda con MailLighter? Preguntas frecuentes, formulario de contacto y compromiso de tiempo de respuesta."
>
  <div class="sup-wrap">
    <!-- LEFT -->
    <div class="sup-left">
      <span class="page-eyebrow">Soporte</span>
      <h1 class="sup-title">¿Necesita ayuda?</h1>
      <p class="sup-sub">Está en el lugar correcto. Consulte las preguntas frecuentes o envíenos un mensaje.</p>

      <div class="sup-faq">
        <h2 class="sup-faq-title">Preguntas frecuentes</h2>
        <div class="sup-faq-list">
          <div class="sup-faq-item">
            <h3 class="sup-faq-q">El botón MailLighter no aparece en mi cinta de Outlook</h3>
            <p class="sup-faq-a">Pruebe a reiniciar Outlook. Si no funciona, compruebe que el complemento esté activado en <em>Archivo → Opciones → Complementos</em> o <em>Inicio → Más aplicaciones → Obtener complementos</em>.</p>
          </div>
          <div class="sup-faq-item">
            <h3 class="sup-faq-q">La limpieza completa elimina cosas que quiero conservar</h3>
            <p class="sup-faq-a">MailLighter conserva las 2 respuestas más recientes por defecto. Utilice la funcionalidad <strong>Conservar la selección</strong> para elegir manualmente lo que se conservará.</p>
          </div>
          <div class="sup-faq-item">
            <h3 class="sup-faq-q">Implementé MailLighter mediante el M365 Admin Center pero los usuarios no lo ven</h3>
            <p class="sup-faq-a">El despliegue centralizado puede tardar hasta 24 horas en propagarse. Los usuarios pueden necesitar reiniciar Outlook una vez.</p>
          </div>
          <div class="sup-faq-item">
            <h3 class="sup-faq-q">Quiero dar comentarios o solicitar una funcionalidad</h3>
            <p class="sup-faq-a">Use el formulario de la derecha o envíe un email a <a href={`mailto:${siteConfig.support.email}`}>{siteConfig.support.email}</a>.</p>
          </div>
        </div>
      </div>

      <div class="sup-meta">
        <p><strong>Tiempo de respuesta:</strong> Respondemos en un plazo de 2 días hábiles. Cada mensaje es leído personalmente.</p>
        <p>
          <a href="/es/docs">Documentación</a> — instalación, despliegue, resolución de problemas.<br />
          <a href="/es/faq">FAQ</a> — privacidad, funcionalidades y despliegue.
        </p>
      </div>
    </div>

    <!-- FORM -->
    <div class="sup-form-wrap">
      <div class="form-title">Contáctenos</div>
      <div class="form-sub">Rellene el formulario y le responderemos lo antes posible.</div>

      <form
        action="https://api.web3forms.com/submit"
        method="POST"
        class="sup-form"
        data-has-access-key={accessKey ? 'true' : 'false'}
      >
        <input type="hidden" name="access_key" value={accessKey} />
        <input type="hidden" name="from_name" value="MailLighter — Soporte" />
        <input type="hidden" name="subject" value="Nueva solicitud de soporte" />
        <div class="hidden" aria-hidden="true">
          <input type="checkbox" name="botcheck" tabindex="-1" autocomplete="off" />
        </div>

        <div class="sup-form-grid">
          <div class="field-group">
            <label for="lastName">Apellido <span class="req">*</span></label>
            <input type="text" id="lastName" name="lastName" required minlength="2" placeholder="García" />
          </div>
          <div class="field-group">
            <label for="email">Email <span class="req">*</span></label>
            <input type="email" id="email" name="email" required placeholder="maria@empresa.com" />
          </div>
          <div class="field-group">
            <label for="requestType">Tipo de solicitud <span class="req">*</span></label>
            <select id="requestType" name="requestType" required>
              <option value="">— Seleccionar —</option>
              <option>Bug</option>
              <option>Solicitud de funcionalidad</option>
              <option>Pregunta</option>
              <option>Otro</option>
            </select>
          </div>
          <div class="field-group">
            <label for="platform">Plataforma Outlook <span class="req">*</span></label>
            <select id="platform" name="platform" required>
              <option value="">— Seleccionar —</option>
              <option>Outlook Desktop Windows</option>
              <option>Outlook Desktop Mac</option>
              <option>Outlook Web</option>
            </select>
          </div>
          <div class="field-group full">
            <label for="message">Descripción <span class="req">*</span></label>
            <textarea id="message" name="message" rows="5" required minlength="10" placeholder="Describa lo que observó y lo que esperaba."></textarea>
          </div>
        </div>

        <button type="submit" class="btn-submit">
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M14 2L2 6l4 4 4 4 4-12zM6 10l2-2"/></svg>
          Enviar solicitud
        </button>
        <p class="form-privacy">🔒 Sus datos nunca se comparten con terceros.</p>
      </form>
    </div>
  </div>
</MarketingLayout>

<style>
  .sup-wrap {
    max-width: 1100px; margin: 0 auto; padding: 120px 52px 100px;
    display: grid; grid-template-columns: 1fr 1.4fr; gap: 80px; align-items: start;
  }
  .page-eyebrow {
    display: inline-flex; align-items: center; gap: 6px;
    background: var(--ml-blue-pale); color: var(--ml-blue);
    font-size: 0.72rem; font-weight: 700; padding: 4px 12px; border-radius: 6px;
    letter-spacing: 0.07em; text-transform: uppercase; margin-bottom: 16px;
  }
  .sup-title {
    font-size: clamp(2rem, 3.5vw, 2.8rem); font-weight: 800;
    color: var(--ml-ink); line-height: 1.1; margin-bottom: 16px;
  }
  .sup-sub { font-size: 1rem; color: var(--ml-ink-soft); line-height: 1.75; margin-bottom: 40px; }

  .sup-faq { margin-bottom: 32px; }
  .sup-faq-title { font-size: 1.15rem; font-weight: 700; color: var(--ml-ink); margin-bottom: 18px; }
  .sup-faq-list { display: flex; flex-direction: column; gap: 16px; }
  .sup-faq-item {
    background: var(--color-surface); border: 1px solid var(--color-border);
    border-radius: 12px; padding: 18px 20px;
    transition: border-color 0.2s, transform 0.2s;
  }
  .sup-faq-item:hover { border-color: var(--ml-blue-light); transform: translateX(4px); }
  .sup-faq-q { font-size: 0.9rem; font-weight: 600; color: var(--ml-ink); margin-bottom: 4px; }
  .sup-faq-a { font-size: 0.82rem; color: var(--ml-ink-soft); line-height: 1.6; }
  .sup-faq-a a { color: var(--ml-blue); text-decoration: none; font-weight: 500; }
  .sup-faq-a a:hover { text-decoration: underline; }

  .sup-meta { font-size: 0.88rem; color: var(--ml-ink-soft); line-height: 1.75; }
  .sup-meta p { margin-bottom: 10px; }
  .sup-meta a { color: var(--ml-blue); text-decoration: none; font-weight: 500; }
  .sup-meta a:hover { text-decoration: underline; }

  .sup-form-wrap {
    background: var(--color-surface); border: 1px solid var(--color-border);
    border-radius: 20px; padding: 40px;
    box-shadow: 0 8px 40px var(--ml-shadow);
  }
  .form-title { font-size: 1.5rem; font-weight: 800; color: var(--ml-ink); margin-bottom: 6px; }
  .form-sub { font-size: 0.87rem; color: var(--ml-ink-soft); margin-bottom: 28px; }

  .sup-form { display: flex; flex-direction: column; gap: 16px; }
  .sup-form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
  .field-group { display: flex; flex-direction: column; gap: 6px; }
  .field-group.full { grid-column: 1 / -1; }
  .field-group label { font-size: 0.85rem; font-weight: 600; color: var(--ml-ink); }
  .req { color: #ef4444; }

  .sup-form input, .sup-form select, .sup-form textarea {
    width: 100%; padding: 10px 14px; border: 1px solid var(--color-border);
    border-radius: 8px; background: white; color: var(--ml-ink);
    font-size: 0.92rem; font-family: inherit;
    transition: border-color 0.2s, box-shadow 0.2s;
  }
  .sup-form input::placeholder, .sup-form textarea::placeholder { color: #b0b8c5; }
  .sup-form textarea { resize: vertical; }
  .sup-form input:focus, .sup-form select:focus, .sup-form textarea:focus {
    outline: none; border-color: var(--ml-blue); box-shadow: 0 0 0 3px rgba(21,100,192,0.15);
  }
  :global(.dark) .sup-form input,
  :global(.dark) .sup-form select,
  :global(.dark) .sup-form textarea { background: var(--color-bg); }

  .btn-submit {
    width: 100%; display: flex; align-items: center; justify-content: center; gap: 8px;
    background: var(--ml-blue); color: white;
    padding: 13px 26px; border-radius: 8px; font-weight: 600; font-size: 0.95rem;
    border: 0; cursor: pointer; box-shadow: 0 4px 16px rgba(21,100,192,0.3);
    transition: background 0.22s, transform 0.22s, box-shadow 0.22s;
  }
  .btn-submit:hover { background: var(--ml-blue-dark); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(21,100,192,0.35); }

  .form-privacy { font-size: 0.78rem; color: var(--ml-ink-mute); text-align: center; margin-top: -4px; }

  .hidden { display: none; }

  @media (max-width: 768px) {
    .sup-wrap { grid-template-columns: 1fr; gap: 40px; padding: 100px 20px 60px; }
  }
  @media (max-width: 640px) {
    .sup-form-grid { grid-template-columns: 1fr; }
  }
</style>
